hipaa.salihedneer.com {
	# API proxy to backend - must come first
	handle /api/* {
		reverse_proxy backend:3001
	}

	# Serve static files with SPA fallback
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
